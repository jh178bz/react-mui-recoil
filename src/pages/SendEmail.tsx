import { FC } from 'react';
import { Layout } from '../components/Layout';

const SendEmail: FC = () => {
    return (
        <Layout>
            <div>SendEmail Page</div>
        </Layout>
    );
};

export default SendEmail;
