import { FC } from 'react';
import { Layout } from '../components/Layout';

const Starred: FC = () => {
    return (
        <Layout>
            <div>Starred Page</div>
        </Layout>
    );
};

export default Starred;
