import { FC } from 'react';
import { Layout } from '../components/Layout';

const Inbox: FC = () => {
    return (
        <Layout>
            <div>Inbox Page</div>
        </Layout>
    );
};

export default Inbox;
