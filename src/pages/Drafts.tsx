import { FC } from 'react';
import { Layout } from '../components/Layout';

const Drafts: FC = () => {
    return (
        <Layout>
            <div>Drafts Page</div>
        </Layout>
    );
};

export default Drafts;
